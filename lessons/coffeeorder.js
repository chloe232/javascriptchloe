/*const coffeeOrder = (size, typeOfDrink) => { 
    console.log ('collecting ${size} from order ${typeOfDrink}');
}

coffeeOrder(large, tea); 

const takeOrder = (size, drinkType) => {
    console.log(`Order received: ${size} ${drinkType}`);
}
takeOrder("tall", "Latte");
This one works! 


const addUp = (num1, num2) => {
    return num1 + num2;
}

addUp(7,3);
console.log(addUp(2,5));
This one works also addition calculator

const multiplyByNineFifths = (celsius) => {
    return celsius * (9/5);
};
const getFahrenheite = (celsius) => {
    return multiplyByNineFifths (celsius) + 32;
};
console.log ("The temperature is" + getFahrenheite (15) + "°F" );
the above shows you the temperature in fahrenheite! */


