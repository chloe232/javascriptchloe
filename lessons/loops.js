/*const order = (top1, top2, top3, top4, top5) => {
console.log (`sandwich order: ${top1}, ${top2}, ${top3}, ${top4}, ${top5});
}

order("Ham", "Sweetcorn", "Tomato", "leutice", "onion");
dosnt work
NEW LINE

for (i = 9 ; i >-1 ; i--){
    console.log(i);
} 
loop for counting down from 9-0 

changing i to =0 and i <9 makes it count up from 0-9. 

let faviroteDrink = [
    "Tango",
    "Ornage",
    "Tea"
];

for (drinkIndex = 0; drinkIndex < faviroteDrink.length; drinkIndex++) {
    console.log(faviroteDrink[drinkIndex]);
}



let faviroteFilm = [
    "Tangled",
    "HTTYD",
    "lionking",
    "Bambi"
];
faviroteFilm.push ("looper", "Rocketman")

for (filmIndex = 0; filmIndex < faviroteFilm.length; filmIndex++) {
    console.log(faviroteFilm[filmIndex]);
}
for look is above 

let cards = ["Diamond", "Spade", "Heart", "Club"];
let currentCard ="";

while(currentCard != "Spade"){
    console.log(currentCard);
    currentCard = cards [Math.floor(Math.random()*4)];
}

let faviroteFilm = [
    "Tangled",
    "HTTYD",
    "lionking",
    "GohstBusters",
    "Bambi"
];

faviroteFilm.push ("looper", "Rocketman")

if (faviroteFilm[3] === "GohstBusters"){
    console.log("found it")
}
else {
    console.log ("booooooo")
}
for (filmIndex = 0; filmIndex < faviroteFilm.length; filmIndex++) {
    console.log(faviroteFilm[filmIndex]);
}

the above works but isnt fully correct see the one on code share for the correct version. */